///1323. Maximum 69 Number

int maximum69Number (int num) {
        string ch=to_string(num);
        for(int i=0;i<ch.size();i++){
            if(ch[i]=='6'){
                ch[i]='9';
                break;//this is important or else wrong answer will be solved
            }
        }
        return stoi(ch);
    }